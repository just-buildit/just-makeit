$ jm status   # exit 1
warning: project generated with just-makeit 0.33.14, running <HEAD> (gh-183) — results may not reflect the pinned version.
MISSING (7) — `jm apply` will create:
  + .clang-tidy
  + CMakePresets.json
  + native/benchmarks/bench_dsp_core.c
  + native/tests/jm_test.h
  + native/tests/test_dsp_core.c
  + native/tests/test_fir_symbols.c
  + native/tests/test_gain_symbols.c

STALE (9) — `jm apply` will rewrite from the manifest:
  ~ CMakeLists.txt
  ~ native/inc/stale.h
  ~ native/src/dsp/CMakeLists.txt
  ~ native/src/dsp/dsp_ext.c
  ~ native/src/fir/CMakeLists.txt
  ~ native/src/gain/CMakeLists.txt
  ~ native/src/gain/gain_ext.c
  ~ src/stale/dsp/dsp.pyi
  ~ src/stale/gain.pyi
  Run `jm apply` to sync.

UNRECONCILED (1) — `jm apply` reconciles these in place, but not wholesale:
  ACTIONABLE (1) — the manifest moved and these did not, so a fix is
  sitting undelivered. Delete the file and re-run `jm apply` to receive it
  (any hand-written body in it is lost).
    ! native/src/dsp/dsp_ext_fir.c
        units: 13 identical, 5 differ
          fn:Fir_dealloc, fn:Fir_decimate, fn:Fir_init, fn:Fir_steps, table:PyMethodDef
        decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
        steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
  What apply will not do is re-render a wrapper body — those are yours, and
  gh-770 is what keeps a hand-written one through a regeneration. So a body
  that no longer matches the manifest is reported (see the `warning:` lines
  above) and left alone.
  None of this counts as drift for `--check`.

OUTDATED (5) — create-only file(s) behind jm's current version:
  ↑ .gitignore
  ↑ Doxyfile
  ↑ Makefile
  ↑ native/benchmarks/jm_bench.h
  ↑ native/inc/clib_common.h
  jm owns the content of these and ships a newer one, but `apply` will NOT
  rewrite a create-only file — adopting it is your call, and your own edits
  to it are here too. Diff before replacing; see docs/upgrading.md. Not counted.

ROOT CMAKE (8) — fixes jm's root CMakeLists.txt template carries that yours lacks:
  ↑ libm (gh-1452): the combined library does not link libm PUBLIC, so a C consumer built through find_package() fails with undefined math symbols
  ↑ static-name (gh-1368) [Windows]: the static library shares the shared library's OUTPUT_NAME, so both produce <name>.lib: `ninja: error: multiple rules generate`
  ↑ export-all (gh-1368) [Windows]: the shared library does not set WINDOWS_EXPORT_ALL_SYMBOLS, so its DLL exports nothing and every C consumer fails to link
  ↑ build-type (gh-1368) [Windows (clang-cl)]: no default CMAKE_BUILD_TYPE before project(), so a bare configure is Debug and links the debug interpreter's python3X_d.lib
  ↑ msvc-runtime (gh-1368) [Windows (clang-cl)]: CMAKE_MSVC_RUNTIME_LIBRARY is not pinned to the release CRT, so a Debug build links python3X_d.lib, which only a debug Python ships
  ↑ complex-range (gh-1368) [Windows (clang-cl)]: no /clang:-fcx-limited-range, so complex multiply and divide link against __mulsc3/__divsc3, which the MSVC link lacks
  ↑ win-defines (gh-1368) [Windows]: the CRT and math defines are missing (_CRT_SECURE_NO_WARNINGS, _CRT_NONSTDC_NO_DEPRECATE, _USE_MATH_DEFINES): M_PI is undefined and every portable C99 call warns
  ↑ install-block (gh-1589): the install section is not jm's managed block (no "# ── End install" line), so no packaging fix reaches it: the soname, the install-time .pc prefix, the macOS install name, the build-tree export. `jm adopt --packaging --check` shows what taking it changes
  Outside its marked blocks the root CMakeLists.txt is yours, so `apply` will
  not add these. `jm status --diff` prints it against today's render to merge
  from. A fix you do not want: name it in [project] status_allow as
  `CMakeLists.txt:<fix>`. Not counted.

PACKAGING (2) — packaging template(s) jm does not own, behind today's render:
  ↑ cmake/stale.pc.in
  ↑ cmake/stale-config.cmake.in
  Without the `# jm:generated` line `apply` never renders these, so no
  packaging fix reaches them. `jm adopt --packaging --check` shows each
  diff; `jm adopt --packaging` hands them to jm. Not counted.

UNWIRED (2) — component core(s) folded into no C library:
  ⊘ dsp_core (native/src/dsp) — missing from stale_lib, stale_lib_static
  ⊘ gain_core (native/src/gain) — missing from stale_lib_static
  These build, and their symbols ship in neither lib<pkg>.so nor lib<pkg>.a,
  so the installed header declares functions a C consumer cannot link. Python
  is unaffected — the extension links each core directly — which is why this
  goes unnoticed. `jm apply` writes the missing target_sources() line. Keep a
  core out of the library on purpose by naming `CMakeLists.txt:<core>` in
  [project] status_allow; naming `CMakeLists.txt` itself exempts every core,
  for a project that links its cores its own way.

SUPERSEDED (1) — file(s) jm has renamed:
  → jb.toml  (bootstrap.toml)
    jb.toml is now called bootstrap.toml; jm will not create bootstrap.toml beside it. Run `just-makeit upgrade` to rename it -- your edits come along.
  Not drift.

summary: 40 OK, 7 missing, 9 stale, 1 unreconciled, 5 outdated, 8 root-cmake, 2 packaging, 2 unwired (!).
Your `_core.c` is yours — apply only ADDS a definition the manifest declares and the file lacks, never rewriting or removing what you wrote; use `jm regenerate <component>` to rebuild one from the manifest.

$ jm apply   # exit 0
warning: project schema is v7, current is v8. Run 'just-makeit upgrade' to get new features.
warning: this project was last generated with just-makeit 0.33.14, but you are running <HEAD>. Generated glue may be incorrect.
         Install the matching version (uvx --from 'just-makeit==0.33.14' just-makeit …) or re-apply with the current CLI to move the project forward.
  stamp   just-makeit.toml  [project] jm_version = <HEAD>

just-makeit: applying just-makeit.toml

  update  native/src/dsp/dsp_ext_fir.c: docstring(s) rewritten from the header for decimate_max_out, destroy
warning ~: native/src/dsp/dsp_ext_fir.c: binding no longer matches the manifest [decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored; steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored]. A sacred fragment only gains missing members on apply, so a changed member stays as written while the .pyi moves — the stub now documents behaviour the extension does not have. Delete native/src/dsp/dsp_ext_fir.c and re-run `just-makeit apply` to regenerate it (any hand-written body in it is lost), or edit the binding to match.
warning ~: jb.toml is now called bootstrap.toml; jm will not create bootstrap.toml beside it. Run `just-makeit upgrade` to rename it -- your edits come along.
  create  .clang-tidy
  create  CMakePresets.json
  create  native/benchmarks/bench_dsp_core.c
  create  native/tests/jm_test.h
  create  native/tests/test_dsp_core.c
  create  native/tests/test_fir_symbols.c
  create  native/tests/test_gain_symbols.c
  update  CMakeLists.txt
  update  native/inc/stale.h
  update  native/src/dsp/dsp_ext.c
  update  native/src/dsp/CMakeLists.txt
  update  src/stale/dsp/dsp.pyi
  update  native/src/fir/CMakeLists.txt
  update  native/src/gain/CMakeLists.txt
  update  native/src/gain/gain_ext.c
  update  src/stale/gain.pyi
  update  native/src/dsp/dsp_ext_fir.c

Done!  Materialized 7 new file(s), patched 0 impl(s), and reconciled 10 wiring file(s) from just-makeit.toml.
warning ~: native/benchmarks/bench_dsp_core.c — records no measurement; jm scaffolded it for this module's 1 function(s) and only you know what to time

$ jm status   # exit 0
UNRECONCILED (1) — `jm apply` reconciles these in place, but not wholesale:
  ACTIONABLE (1) — the manifest moved and these did not, so a fix is
  sitting undelivered. Delete the file and re-run `jm apply` to receive it
  (any hand-written body in it is lost).
    ! native/src/dsp/dsp_ext_fir.c
        units: 13 identical, 5 differ
          fn:Fir_dealloc, fn:Fir_decimate, fn:Fir_init, fn:Fir_steps, table:PyMethodDef
        decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
        steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
  What apply will not do is re-render a wrapper body — those are yours, and
  gh-770 is what keeps a hand-written one through a regeneration. So a body
  that no longer matches the manifest is reported (see the `warning:` lines
  above) and left alone.
  None of this counts as drift for `--check`.

OUTDATED (5) — create-only file(s) behind jm's current version:
  ↑ .gitignore
  ↑ Doxyfile
  ↑ Makefile
  ↑ native/benchmarks/jm_bench.h
  ↑ native/inc/clib_common.h
  jm owns the content of these and ships a newer one, but `apply` will NOT
  rewrite a create-only file — adopting it is your call, and your own edits
  to it are here too. Diff before replacing; see docs/upgrading.md. Not counted.

ROOT CMAKE (8) — fixes jm's root CMakeLists.txt template carries that yours lacks:
  ↑ libm (gh-1452): the combined library does not link libm PUBLIC, so a C consumer built through find_package() fails with undefined math symbols
  ↑ static-name (gh-1368) [Windows]: the static library shares the shared library's OUTPUT_NAME, so both produce <name>.lib: `ninja: error: multiple rules generate`
  ↑ export-all (gh-1368) [Windows]: the shared library does not set WINDOWS_EXPORT_ALL_SYMBOLS, so its DLL exports nothing and every C consumer fails to link
  ↑ build-type (gh-1368) [Windows (clang-cl)]: no default CMAKE_BUILD_TYPE before project(), so a bare configure is Debug and links the debug interpreter's python3X_d.lib
  ↑ msvc-runtime (gh-1368) [Windows (clang-cl)]: CMAKE_MSVC_RUNTIME_LIBRARY is not pinned to the release CRT, so a Debug build links python3X_d.lib, which only a debug Python ships
  ↑ complex-range (gh-1368) [Windows (clang-cl)]: no /clang:-fcx-limited-range, so complex multiply and divide link against __mulsc3/__divsc3, which the MSVC link lacks
  ↑ win-defines (gh-1368) [Windows]: the CRT and math defines are missing (_CRT_SECURE_NO_WARNINGS, _CRT_NONSTDC_NO_DEPRECATE, _USE_MATH_DEFINES): M_PI is undefined and every portable C99 call warns
  ↑ install-block (gh-1589): the install section is not jm's managed block (no "# ── End install" line), so no packaging fix reaches it: the soname, the install-time .pc prefix, the macOS install name, the build-tree export. `jm adopt --packaging --check` shows what taking it changes
  Outside its marked blocks the root CMakeLists.txt is yours, so `apply` will
  not add these. `jm status --diff` prints it against today's render to merge
  from. A fix you do not want: name it in [project] status_allow as
  `CMakeLists.txt:<fix>`. Not counted.

PACKAGING (2) — packaging template(s) jm does not own, behind today's render:
  ↑ cmake/stale.pc.in
  ↑ cmake/stale-config.cmake.in
  Without the `# jm:generated` line `apply` never renders these, so no
  packaging fix reaches them. `jm adopt --packaging --check` shows each
  diff; `jm adopt --packaging` hands them to jm. Not counted.

SILENT (1) — benchmark(s) that record no measurement:
  ~ native/benchmarks/bench_dsp_core.c (1 function(s), none timed yet)
  These build, run, exit 0 and write an empty "benchmarks": []. Not drift.

SUPERSEDED (1) — file(s) jm has renamed:
  → jb.toml  (bootstrap.toml)
    jb.toml is now called bootstrap.toml; jm will not create bootstrap.toml beside it. Run `just-makeit upgrade` to rename it -- your edits come along.
  Not drift.

OK — up to date; 56 manifest-owned file(s) match; 1 unreconciled; 1 silent bench; 5 outdated; 8 root-cmake; 2 packaging.
  create-only files: jm's own — the Makefile, .clang-tidy, jm_test.h,
        jm_bench.h, jm_perf.h, the common headers — are compared against
        this jm's render and reported as OUTDATED. NOT compared: the ones
        whose content is yours — _core.c, your C and Python tests, README,
        pyproject.toml — which differ from their scaffold as soon as the
        project is real. `apply` rewrites neither kind; see docs/upgrading.md.

$ jm apply   # exit 0
warning: project schema is v7, current is v8. Run 'just-makeit upgrade' to get new features.
just-makeit: applying just-makeit.toml

warning ~: native/src/dsp/dsp_ext_fir.c: binding no longer matches the manifest [decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored; steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored]. A sacred fragment only gains missing members on apply, so a changed member stays as written while the .pyi moves — the stub now documents behaviour the extension does not have. Delete native/src/dsp/dsp_ext_fir.c and re-run `just-makeit apply` to regenerate it (any hand-written body in it is lost), or edit the binding to match.
warning ~: jb.toml is now called bootstrap.toml; jm will not create bootstrap.toml beside it. Run `just-makeit upgrade` to rename it -- your edits come along.
  update  CMakeLists.txt

Done!  Materialized 0 new file(s), patched 0 impl(s), and reconciled 1 wiring file(s) from just-makeit.toml.
warning ~: native/benchmarks/bench_dsp_core.c — records no measurement; jm scaffolded it for this module's 1 function(s) and only you know what to time

$ jm upgrade   # exit 0
migrating schema 7 → 8
  move    native/inc/* -> native/inc/stale/  (6 file(s))
  update  native/benchmarks/bench_dsp_core.c  (header references)
  update  native/benchmarks/bench_fir_core.c  (header references)
  update  native/benchmarks/bench_gain_core.c  (header references)
  update  native/inc/stale/dsp/dsp_core.h  (header references)
  update  native/inc/stale/fir/fir_core.h  (header references)
  update  native/inc/stale/gain/gain_core.h  (header references)
  update  native/inc/stale/stale.h  (header references)
  update  native/src/dsp/dsp_core.c  (header references)
  update  native/src/dsp/dsp_ext.c  (header references)
  update  native/src/dsp/dsp_ext_fir.c  (header references)
  update  native/src/dsp/energy.c  (header references)
  update  native/src/fir/fir_core.c  (header references)
  update  native/src/gain/gain_core.c  (header references)
  update  native/src/gain/gain_ext.c  (header references)
  update  native/tests/jm_test.h  (header references)
  update  native/tests/test_dsp_core.c  (header references)
  update  native/tests/test_fir_core.c  (header references)
  update  native/tests/test_fir_symbols.c  (header references)
  update  native/tests/test_gain_core.c  (header references)
  update  native/tests/test_gain_symbols.c  (header references)
  update  native/src/fir/CMakeLists.txt  (header references)
  update  native/src/gain/CMakeLists.txt  (header references)
project is now at schema 8

renamed jb.toml -> bootstrap.toml; your edits came along.

respelled the complex types in 4 file(s) (`complex` -> `_Complex`, gh-1246):
  native/inc/stale/fir/fir_core.h
  native/src/fir/fir_core.c
  native/tests/test_fir_core.c
  native/tests/test_gain_core.c
  Same type, same ABI -- identical tokens after preprocessing.
  Rebuild and run your tests. If you adopted jm's new clib_common.h,
  a C++11 caller can use `std::complex` again.

$ jm status   # exit 0
UNRECONCILED (1) — `jm apply` reconciles these in place, but not wholesale:
  ACTIONABLE (1) — the manifest moved and these did not, so a fix is
  sitting undelivered. Delete the file and re-run `jm apply` to receive it
  (any hand-written body in it is lost).
    ! native/src/dsp/dsp_ext_fir.c
        units: 13 identical, 5 differ
          fn:Fir_dealloc, fn:Fir_decimate, fn:Fir_init, fn:Fir_steps, table:PyMethodDef
        decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
        steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored
  What apply will not do is re-render a wrapper body — those are yours, and
  gh-770 is what keeps a hand-written one through a regeneration. So a body
  that no longer matches the manifest is reported (see the `warning:` lines
  above) and left alone.
  None of this counts as drift for `--check`.

OUTDATED (6) — create-only file(s) behind jm's current version:
  ↑ .gitignore
  ↑ Doxyfile
  ↑ Makefile
  ↑ bootstrap.toml
  ↑ native/benchmarks/jm_bench.h
  ↑ native/inc/stale/clib_common.h
  jm owns the content of these and ships a newer one, but `apply` will NOT
  rewrite a create-only file — adopting it is your call, and your own edits
  to it are here too. Diff before replacing; see docs/upgrading.md. Not counted.

PACKAGING (2) — packaging template(s) jm does not own, behind today's render:
  ↑ cmake/stale.pc.in
  ↑ cmake/stale-config.cmake.in
  Without the `# jm:generated` line `apply` never renders these, so no
  packaging fix reaches them. `jm adopt --packaging --check` shows each
  diff; `jm adopt --packaging` hands them to jm. Not counted.

SILENT (1) — benchmark(s) that record no measurement:
  ~ native/benchmarks/bench_dsp_core.c (1 function(s), none timed yet)
  These build, run, exit 0 and write an empty "benchmarks": []. Not drift.

OK — up to date; 56 manifest-owned file(s) match; 1 unreconciled; 1 silent bench; 6 outdated; 2 packaging.
  create-only files: jm's own — the Makefile, .clang-tidy, jm_test.h,
        jm_bench.h, jm_perf.h, the common headers — are compared against
        this jm's render and reported as OUTDATED. NOT compared: the ones
        whose content is yours — _core.c, your C and Python tests, README,
        pyproject.toml — which differ from their scaffold as soon as the
        project is real. `apply` rewrites neither kind; see docs/upgrading.md.

$ jm adopt --packaging --check   # exit 0
adopt --packaging --check — jm owns cmake/'s packaging templates where nothing is lost
  would adopt         cmake/stale.pc.in (takes the render)
--- a/cmake/stale.pc.in
+++ b/cmake/stale.pc.in (jm's render)
@@ -1,10 +1,14 @@
-prefix=@CMAKE_INSTALL_PREFIX@
+# jm:generated stale.pc.in
+# just-makeit regenerates the project's cmake/ template of this file on
+# apply while the line above is there; deleting that line there makes
+# the template the project's own.
+prefix=@JM_PC_PREFIX@
 exec_prefix=${prefix}
-libdir=${exec_prefix}/@CMAKE_INSTALL_LIBDIR@
-includedir=${prefix}/@CMAKE_INSTALL_INCLUDEDIR@
+libdir=@JM_PC_LIBDIR@
+includedir=@JM_PC_INCLUDEDIR@
 
 Name: stale
-Description: stale C library
-Version: @PROJECT_VERSION@
-Libs: -L${libdir} -lstale
-Cflags: -I${includedir}
+Description: @PROJECT_DESCRIPTION@
+@JM_PC_EXTRA_FIELDS@Version: @PROJECT_VERSION@
+Libs: -L${libdir} -lstale@JM_PC_LIBM@
+Cflags: -I${includedir}@JM_PC_CFLAGS@
  would adopt         cmake/stale-config.cmake.in (takes the render)
--- a/cmake/stale-config.cmake.in
+++ b/cmake/stale-config.cmake.in (jm's render)
@@ -1,4 +1,12 @@
+# jm:generated stale-config.cmake.in
+# just-makeit regenerates the project's cmake/ template of this file on
+# apply while the line above is there; deleting that line there makes
+# the template the project's own.
 @PACKAGE_INIT@
+
+# gh-1572: the packages whose targets the exported libraries link, from
+# `[project] find_packages` / `pkg_modules`; empty when there are none.
+@JM_FIND_DEPENDENCIES@
 
 include("${CMAKE_CURRENT_LIST_DIR}/stale-targets.cmake")
 
  jm's already        CMakeLists.txt

$ jm adopt --packaging   # exit 0
adopt --packaging — jm owns cmake/'s packaging templates where nothing is lost
  adopted             cmake/stale.pc.in (takes the render)
  adopted             cmake/stale-config.cmake.in (takes the render)
  jm's already        CMakeLists.txt

$ jm apply   # exit 0
just-makeit: applying just-makeit.toml

warning ~: native/src/dsp/dsp_ext_fir.c: binding no longer matches the manifest [decimate: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored; steps: jm's out= guard tests C-contiguity and this fragment does not, so a strided out= is filled through a copy and silently ignored]. A sacred fragment only gains missing members on apply, so a changed member stays as written while the .pyi moves — the stub now documents behaviour the extension does not have. Delete native/src/dsp/dsp_ext_fir.c and re-run `just-makeit apply` to regenerate it (any hand-written body in it is lost), or edit the binding to match.
  create  .gitignore
  create  Doxyfile
  create  Makefile
  create  bootstrap.toml
  create  native/benchmarks/bench_fir_core.c
  create  native/benchmarks/bench_gain_core.c
  create  native/benchmarks/jm_bench.h
  create  native/inc/stale/clib_common.h

Done!  Materialized 8 new file(s), patched 0 impl(s), and reconciled 0 wiring file(s) from just-makeit.toml.
warning ~: native/benchmarks/bench_dsp_core.c — records no measurement; jm scaffolded it for this module's 1 function(s) and only you know what to time

$ jm adopt --check   # exit 1
adopt --check — would `fragment = "generated"` be safe?
  needs acknowledgement fir
      native/src/dsp/dsp_ext_fir.c
        differs:   fn:Fir_dealloc
        differs:   fn:Fir_decimate
        differs:   fn:Fir_init
        adds only: fn:Fir_steps
        adds only: table:PyMethodDef

  0 of 1 object(s) could flip
  A unit that exists ONLY on disk refuses the flip: the render
  does not produce it, so flipping would delete it. Move it to
  the `_extra.c` beside the fragment, which is already wired.
  A member whose BINDING accepts more than the manifest
  declares refuses too: there the file is ahead, so flipping
  removes a feature. Declare it in the manifest first.
  A unit that DIFFERS needs your eyes: jm cannot tell a
  hand-written body from a render that predates a codegen
  change, which is why it asks rather than guesses.
  `adds only`: the render keeps every token of the unit and
  adds code (a guard, keywords) -- nothing on disk is lost.
  `jm adopt <obj> --accept-additions` takes those; a `differs`
  unit removes code, and is taken only by name:
  `--accept <unit>`.

$ jm apply   # exit 0
just-makeit: applying just-makeit.toml

  create  native/src/dsp/dsp_ext_fir.c

Done!  Materialized 1 new file(s), patched 0 impl(s), and reconciled 0 wiring file(s) from just-makeit.toml.
warning ~: native/benchmarks/bench_dsp_core.c — records no measurement; jm scaffolded it for this module's 1 function(s) and only you know what to time

$ jm adopt --check   # exit 0
adopt --check — would `fragment = "generated"` be safe?
  would flip           fir

  1 of 1 object(s) could flip

$ jm apply   # exit 0
just-makeit: applying just-makeit.toml

  update  native/src/dsp/dsp_ext_fir.c

Done!  Materialized 0 new file(s), patched 0 impl(s), and reconciled 1 wiring file(s) from just-makeit.toml.
warning ~: native/benchmarks/bench_dsp_core.c — records no measurement; jm scaffolded it for this module's 1 function(s) and only you know what to time

$ jm status --check   # exit 0
SILENT (1) — benchmark(s) that record no measurement:
  ~ native/benchmarks/bench_dsp_core.c (1 function(s), none timed yet)
  These build, run, exit 0 and write an empty "benchmarks": []. Not drift.

OK — up to date; 57 manifest-owned file(s) match; 1 silent bench.
  create-only files: jm's own — the Makefile, .clang-tidy, jm_test.h,
        jm_bench.h, jm_perf.h, the common headers — are compared against
        this jm's render and reported as OUTDATED. NOT compared: the ones
        whose content is yours — _core.c, your C and Python tests, README,
        pyproject.toml — which differ from their scaffold as soon as the
        project is real. `apply` rewrites neither kind; see docs/upgrading.md.
