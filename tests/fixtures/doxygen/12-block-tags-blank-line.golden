# fixture: 12-block-tags-blank-line

extracted: ['demo_step']

brief:    'Decimate a CF32 block. Each sample is pushed through the integrators.'
body:     []
params:   [('x', 'input block')]
dirs:     {}
returns:  'decimated block'
examples: []
tags:     [('note', '**Input amplitude is bounded.** A component beyond the range is clipped at the boundary before filtering.')]

rendered:
        """Decimate a CF32 block. Each sample is pushed through the integrators.

        Parameters
        ----------
        x : float
            input block

        Returns
        -------
        float
            decimated block

        Notes
        -----
        **Input amplitude is bounded.** A component beyond the range is clipped
        at the boundary before filtering.
        """
