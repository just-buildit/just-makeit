/**
 * @brief Filter one sample.
 *
 * @param x  An authored parameter description long enough that emitting it
 *           verbatim on one line would push the generated stub well past the
 *           column budget (gh-678).
 * @return A return description that is likewise longer than one line, so both
 *         wrapped slots are pinned by this fixture rather than only the one.
 */
float demo_step(demo_state_t *state, float x);
