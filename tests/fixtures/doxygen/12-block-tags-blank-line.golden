# fixture: 12-block-tags-blank-line

extracted: ['demo_step']

brief:    'Decimate a CF32 block. Each sample is pushed through the integrators.'
body:     ['@note **Input amplitude is bounded.** A component beyond the range', 'is clipped at the boundary before filtering.']
params:   [('x', 'input block')]
dirs:     {}
returns:  'decimated block'
examples: []
tags:     []

rendered:
        """Decimate a CF32 block. Each sample is pushed through the integrators.

        @note **Input amplitude is bounded.** A component beyond the range is
        clipped at the boundary before filtering.

        Parameters
        ----------
        x : float
            input block

        Returns
        -------
        float
            decimated block
        """
